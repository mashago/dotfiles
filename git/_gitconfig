[color]
    diff = auto
    ui = true
[push]
    default = simple
[alias]
    st = status
    co = checkout
    br = branch
    mg = merge
    ci = commit
    chp = cherry-pick
[pull]
    rebase = true
    stat = true
[mergetool]
    prompt = false
[mergetool "vimdiff"]
    cmd = vim -d "$LOCAL" "$MERGED" "$REMOTE" -c '2wincmd w'
